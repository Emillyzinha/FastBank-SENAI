import { StyleSheet } from 'react-native'

const stylesTopStart = StyleSheet.create({
    image: {
        width: 200,
        height: 200,
        marginTop: 30,
        display: 'flex',
    },
    inputs: {
        padding: 6
    },
    title: {
        fontSize: 50,
        fontWeight: 400,
        textAlign: 'center',
    }
})

export default stylesTopStart